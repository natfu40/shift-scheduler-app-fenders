name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    name: Build and Test

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Start Docker for TestContainers
      run: |
        sudo systemctl start docker
        docker --version

    - name: Run tests
      run: mvn clean test

    - name: Build application
      run: mvn clean package -DskipTests

    - name: List test report files (debug)
      if: always()
      run: |
        echo "Contents of target directory:"
        ls -la target/ || true
        echo "Contents of surefire-reports directory:"
        ls -la target/surefire-reports/ || true
        echo "Specifically looking for XML files:"
        find target/surefire-reports -name "*.xml" -type f || true
        echo "Count of TEST-*.xml files:"
        find target/surefire-reports -name "TEST-*.xml" -type f | wc -l || true
        echo "Working directory:"
        pwd
        echo "Full path check:"
        ls -la target/surefire-reports/TEST-*.xml || echo "No TEST-*.xml files found"

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: |
          target/surefire-reports/
          target/site/jacoco/

    - name: Publish Test Results
      uses: EnricoMi/publish-unit-test-result-action@v2
      if: success() || failure()
      with:
        files: target/surefire-reports/TEST-*.xml
        check_name: Maven Tests

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: jar-artifact
        path: target/*.jar

  deploy:
    needs: [test]
    runs-on: ubuntu-latest
    name: Build and Deploy
    if: github.ref == 'refs/heads/main'

    steps:
    - uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: jar-artifact
        path: target/

    - name: Build Docker image
      run: |
        docker build -f deployment/Dockerfile -t shift-scheduler-backend .

    - name: List Docker images
      run: docker images shift-scheduler-backend

